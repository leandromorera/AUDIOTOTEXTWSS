# === AUDIO CAPTURE (SENDER) CONFIG ===
# Set the WebSocket URL of the MESSAGE RECEIVER (Machine B)
# Example below uses receiver IP 192.168.0.109 on port 8765
WS_URL=ws://192.168.0.110:8765

# Whisper model size: tiny/base/small/medium/large
WHISPER_MODEL=base

# Seconds of audio captured per chunk
CHUNK_SEC=5

# Optional: specific loopback device name.
# Windows: leave empty (default speaker loopback used).
# macOS: e.g., BlackHole 2ch
# Linux (Pulse/PipeWire): e.g., alsa_output.pci-0000_00_1f.3.analog-stereo.monitor
LOOPBACK_DEVICE=
